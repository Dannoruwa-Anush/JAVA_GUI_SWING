-- -------------------------------
CREATE TABLE IF NOT EXISTS user_roles(
	role_id INT AUTO_INCREMENT,
	role_name VARCHAR(100) NOT NULL,
	PRIMARY KEY(role_id)
);
-- -----------------------------------------------------